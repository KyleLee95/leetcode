class Solution {
  /**
   * @param {character[][]} board
   * @return {boolean}
   */
  isValidSudoku(board) {
    const boardMap = new Map();
    for (let i = 0; i < hello.length; j++) {}
  }
}

const s = new Solution();
s.isValidSudoku();
